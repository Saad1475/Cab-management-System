# Cab-management-System
A menu driven, console based cab booking system build in core Java, simulating the operations of a ride-hailing service through three role-based portals: Admin,Driver, and Passenger.The system uses parallel arrays and array list to manage in-memory data for cabs,drivers, passengers and trip history no external database or external libraries are required. 
